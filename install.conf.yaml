- defaults:
    link:
      relink:  true

- clean: ['~']

- link:
    ~/.bashrc: bashrc
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.tmux.conf: tmux.conf
    ~/.tmux/plugins/tpm/tpm:
      create: true
      path: tpm

- shell:
  - [git submodule update --init --recursive, Installing submodules]

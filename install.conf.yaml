- defaults:
    link:
      relink: true
      create: true

- clean: ['~', '~/.oh-my-zsh/custom/']

- shell:
    - [sh prereqs.sh, Running prerequisite install script]
    - [mkdir -p ~/.oh-my-zsh/custom/themes, Creating oh-my-zsh custom theme folders]

- link:
    ~/.bashrc: bashrc
    ~/.zshrc:
      force: true
      path: zshrc
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.tmux.conf: tmux.conf
    ~/.tmux/plugins/tpm/tpm:
      create: true
      path: tpm
    ~/.oh-my-zsh/custom/themes/powerlevel9k.zsh-theme: oh-my-zsh/custom/themes/powerlevel9k/powerlevel9k.zsh-theme

- shell:
  - [git submodule update --init --recursive, Installing submodules]
